language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gbmp' ]; then cd .. && eval "mv $currentfolder generator-gbmp" && cd generator-gbmp; fi

